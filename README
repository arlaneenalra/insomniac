To setup an build environment, run setup.sh.  If all goes well, you will find a build tree configured in the directory build.

Well, I'm putting this on hold for a bit while waiting for peter to get going.  