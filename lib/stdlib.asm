;;; A few standard library functions that
;;; are needed all over the place
        ;; These symbols should not be accessible
        ;; from scheme directly, so they start with
        ;; a space

        "Hello"
        out
        #\newline
        out